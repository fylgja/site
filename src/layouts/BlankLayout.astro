---
import "@/assets/main.css";
import siteConfig from "site.config";
import defaultSocialImage from "@/assets/default-social-img.png";
import siteLogo from "@/assets/logo.svg";
import Head from "@/components/Head.astro";
import Schema from "@/components/Schema.astro";
import SchemaCrumbs from "@/components/SchemaCrumbs.astro";
import Speculation from "@/components/Speculation.astro";
import type { SpeculationType } from "@/components/Speculation.astro";
import ServiceWorker from "@/components/ServiceWorker.astro";

interface Props {
	title: string;
	description?: string;
	image?: string;
	pageType?: "website" | "article";
	lang?: string;
	dir?: "ltr" | "rtl";
	speculationrules?: {
		prerender?: SpeculationType[];
		prefetch?: SpeculationType[];
	};
}

const { name, description: siteDescription, foundingDate, socials, twitterUser, themeColor, themeColorDark, author } = siteConfig;
const isProd = import.meta.env.PROD;

const {
	title,
	description = siteDescription,
	image = defaultSocialImage.src,
	pageType = "website",
	lang = "en",
	dir = "ltr",
	speculationrules,
} = Astro.props;
---

<html lang={lang} dir={dir} data-theme="auto">
	<Head
		siteName={name}
		title={title}
		description={description}
		image={image}
		themeColor={themeColor}
		themeColorDark={themeColorDark}
		ogType={pageType}
		twitterUser={twitterUser}
	>
		<meta name="generator" content="Fylgja CSS" />
		<meta name="fediverse:creator" content="@fylgja@fosstodon.org" />
		{Object.values(socials).map((item) => <link href={item} rel="me" />)}
		<link
			rel="alternate"
			type="application/rss+xml"
			title=""
			href={new URL("rss.xml", Astro.site)}
		/>
		<link rel="sitemap" href="/sitemap-index.xml" />
		<Schema
			data={{
				"@context": "https://schema.org",
				"@type": "Person",
				"@id": `${author.url}/#person`,
				name: author.name,
				url: author.url,
				jobTitle: author.jobTitle,
				description: author.description,
				sameAs: author.sameAs,
				brand: {
					"@type": "Brand",
					name,
					url: Astro.site,
				},
				knowsAbout: author.knowsAbout,
			}}
		/>
		<Schema
			data={{
				"@context": "https://schema.org",
				"@type": "WebSite",
				name,
				url: Astro.site,
				description,
				author: {
					"@type": "Person",
					"@id": `${author.url}/#person`,
					name: author.name,
				},
			}}
		/>
		<Schema
			data={{
				"@context": "https://schema.org",
				"@type": "Organization",
				url: Astro.site,
				logo: siteLogo.src,
				name,
				alternateName: "Fylgja CSS",
				description,
				foundingDate,
				founder: {
					"@type": "Person",
					"@id": `${author.url}/#person`,
					name: author.name,
					url: author.url,
				},
				sameAs: Object.values(socials),
			}}
		/>
		<SchemaCrumbs />
		<slot name="head" />
		<Speculation rules={speculationrules} />
		<script is:inline>
			function getColorScheme() {
				const preference = localStorage.getItem("theme-preference");
				return preference
					? preference
					: window.matchMedia("(prefers-color-scheme: dark)").matches
						? "dark"
						: "light";
			}

			document.documentElement.setAttribute("data-theme", getColorScheme());
		</script>
		{isProd && <ServiceWorker />}
	</Head>
	<body>
		<slot />
	</body>
</html>
