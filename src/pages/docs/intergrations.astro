---
import Layout from "@/layouts/PageLayout.astro";
const url = new URL("/docs/intergrations/autocomplete", Astro.url);
const title = `Redirecting to: ${url}`;
---

<Layout title={title} redirect={url} />
