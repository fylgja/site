---
layout: default
---

{% include "header.njk" %}
<main id="page-main" class="flex-grow">
    <header class="section aria-only">
        <div class="container">
            {% if title %}
                <h1>{{ title }}</h1>
            {% endif %}
            {% if description %}
                <p>{{ description }}</p>
            {% endif %}
        </div>
    </header>
    {{ content | safe }}
</main>
{% include "footer.njk" %}
