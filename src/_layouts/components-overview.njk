---
layout: default
---

{% include "header.njk" %}
<main class="flex-grow-1">
    <header class="section py-6">
        <div class="container">
            {% if title %}
                <h1>{{ title }}</h1>
            {% endif %}
            {% if description %}
                <p>{{ description }}</p>
            {% endif %}
        </div>
    </header>
    {{ content | safe }}
</main>
{% include "footer.njk" %}
