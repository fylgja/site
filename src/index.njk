---
layout: home
permalink: index.html
style: {
    avatar: "css/avatar-blob.css"
}
sitemap:
  priority: 0.8
---

<header id="hero" aria-label="Welcome to Fylgja the modular CSS framework" class="section flex flex-col">
    {% if navigation.home %}
        <nav aria-label="quick menu" class="hero-nav container p-2 md-py-4 md-px-5 flex justify-end items-center">
            {% for item in navigation.home %}
                <a href="{{ item.url }}" {% if item.external %} rel="noopener noreferrer" target="_blank" {% endif %} {% if item.alt %} aria-label="{{ item.alt }}" {% endif %} class="inline-flex items-center py-1 mx-2 md-mx-4 text-reset{{ ' ' if item.classList }}{{ item.classList }}">
                    {% if item.icon %}
                        {%- set introIcon -%}/src/assets/images/icons/{{ item.icon }}.svg{%- endset -%}
                        {{ introIcon | svgContents | safe }}
                    {% endif %}
                    <div>{{ item.text }}</div>
                </a>
            {% endfor %}
            <a
                href="https://github.com/fylgja/fylgja"
                rel="noopener noreferrer"
                target="_blank"
                title="Visit the Fylgja Github repo"
                aria-label="Visit the Fylgja Github repo"
                class="ms-4 me-2 btn -icon">{{ "/src/assets/images/icons/github.svg" | svgContents | safe }}</a>
            {% include "theme-toggle.njk" %}
        </nav>
    {% endif %}
    <div class="hero-content container my-auto flex flex-col lg-flex-row lg-justify-between items-center">
        <div class="text-center mb-12 mx-2 md-mx-4">
            <h1 class="inline-block h4 mt-0 mb-4 logo -large">
                {{ "/src/assets/images/logo.svg" | svgContents | safe }}
                <span class="block subheading mt-4">The modular highly customizable CSS framework.</span>
            </h1>
            <p class="mb-6">
                <a href="/getting-started/" class="btn -theme">Get started</a>
                <a href="/components/" class="btn -outline ms-2 hidden md-inline-flex">Components</a>
            </p>
            <p class="my-0 md-aria-only">Or explore the full range of <a href="/components/">Fylgja's Components →</a></p>
        </div>
        <div class="code-box-grid">
            <div class="code-box">
                <div class="code-box-header">
                    <span class="osx-close"></span>
                    <span class="lang">SCSS</span>
                </div>
                {% include "code-sample/scss.njk" %}
            </div>
            <div class="code-box">
                <div class="code-box-header">
                    <span class="osx-close"></span>
                    <span class="lang">PostCSS Import</span>
                </div>
                {% include "code-sample/postcss.njk" %}
            </div>
        </div>
    </div>
</header>
<section id="features" aria-label="The experiences you get from using Fylgja" class="container py-6 md-py-8">
    <div id="intro" class="pt-12 pb-6">
        <h2 class="h1">The experiences you get from using Fylgja</h2>
        <p>Fylgja is a highly flexible CSS framework that brings together the best features from other frameworks.</p>
        <p>With Fylgja, you have the option to use it as is or customize it to fit your unique needs with your own CSS.</p>
        <p>
            Whether you're looking to enhance your current stack or build a new project from scratch,<br>
            Fylgja is the ideal solution.
        </p>
    </div>
    <ul id="features-list" class="auto-grid py-12 px-0">
        {% for item in usps.home %}
            <li class="flex{{ ' ' if item.classList }}{{ item.classList }}">
                {% if item.icon %}
                    {%- set listIcon -%}/src/assets/images/icons/{{ item.icon }}.svg{%- endset -%}
                    <div class="avatar -blob me-6 lg-me-10" style="{{ item.iconStyle }}">
                        {{ listIcon | svgContents | safe }}
                    </div>
                {% endif %}
                <div>
                    <h3>{{ item.title | safe }}</h3>
                    <p>{{ item.content | safe }}</p>
                </div>
            </li>
        {% endfor %}
    </ul>
</section>
{% if collections.componentsFeatured %}
    <section
        id="explore-fylgja"
        aria-label="Explore all Fylgja Components"
        class="collection-row py-12 my-4 md-my-8"
    >
        <div class="container md-py-8">
            <div>
                <h2 class="mb-1">Our Featured CSS Components</h2>
                <p>Our Featured CSS Components, that really show the full potential of what each Fylgja Component can do.</p>
            </div>
            <div class="auto-grid my-8">
                {%- for item in collections.componentsFeatured | sortByOrder | limit(4) -%}
                    <a class="card flex-row md-flex-col" href="{{ item.url }}">
                        <div class="card-media avatar border md-border-0 my-4 ms-4 md-m-0 aspect-ratio">
                            {% if item.data.preview %}
                                {% set preview %}{{ meta.componentsMedia }}{{ item.data.preview }}{% endset %}
                                {% image preview, item.data.title, "medium" %}
                            {% endif %}
                        </div>
                        <div class="card-content">
                            <h3 class="h5 my-0">{{ item.data.title }}</h3>
                            {% if item.data.description %}
                                <p class="text-small">{{ item.data.description | truncate(strLimit) }}</p>
                            {% else %}
                                <p class="text-small">{{ item.templateContent | truncate(strLimit) }}</p>
                            {% endif %}
                        </div>
                    </a>
                {%- endfor -%}
            </div>
            <div class="text-center mt-12">
                <p>Want to explore what Fylgja has the offer, checkout the Components page</p>
                <p><a href="/components" class="btn -theme">Browse all Fylgja Components</a></p>
            </div>
        </div>
    </section>
{% endif %}
{% if support %}
    <section id="contact" aria-label="Get involved with Fylgja" class="flex flex-col justify-center py-12">
        <div class="container">
            <div class="lg-text-center mb-5 md-mb-7">
                <h2>Get involved on</h2>
            </div>
            <div class="flex flex-col flex-wrap gap-8 lg-flex-row lg-justify-center">
                {% for item in support %}
                    <a href="{{ item.url }}" {% if item.external %} rel="noopener noreferrer" target="_blank" {% endif %} class="text-reset no-underline flex iteme-start gap-4">
                        {% if item.icon %}
                            <div class="avatar p-2" style="--avatar-bg: {{ item.iconStyle.bg }}; --avatar-color: {{ item.iconStyle.color }};">
                                <svg width="24" height="24" fill="currentcolor">
                                    <use href="{{ '/images/socials.svg' | assetUrl }}#{{ item.icon }}"/>
                                </svg>
                            </div>
                        {% endif %}
                        <div class="flex-auto">
                            <h3 class="h5 mt-2 mb-0">{{ item.title }}</h3>
                            <p>{{ item.text | safe }}</p>
                        </div>
                    </a>
                {% endfor %}
            </ul>
        </div>
    </section>
{% endif %}
