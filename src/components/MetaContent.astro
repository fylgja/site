---
import { parseDate } from "@/utils";

interface Props {
	published: Date;
	updatedOn?: Date;
}

const { published, updatedOn } = Astro.props;
---

<header class="meta">
	{published && <time datetime={String(published)}>{parseDate(published)}</time>}
	{updatedOn && <time datetime={String(updatedOn)}>{parseDate(updatedOn)}</time>}
</header>

<style>
	.meta {
		display: inline-flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.5rem 1rem;
		font-size: 0.875em;
		color: var(--text-color-muted);
	}

	.meta a {
		display: flex;
		align-items: center;
		gap: 0.375rem;
		text-decoration: none;
		color: color-mix(in srgb, var(--brand) 65%, var(--root-fg));

		& svg {
			color: var(--text-color-muted);
		}

		&:hover {
			color: var(--text-color);
		}
	}
</style>
