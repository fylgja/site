---
interface Props {
	title?: string;
	body?: string;
	style?: string;
	styleContent?: string;
}

const { title, body, style, styleContent } = Astro.props;
---

<div class="card border-gradient" style={style}>
	<div class="card-content" style={styleContent}>
		<slot>
			<h3>{title}</h3>
			<p>{body}</p>
		</slot>
	</div>
</div>

<style>
	.card {
		--gradient-from: var(--brand);
		--gradient-to: var(--color-5);
		--gradient-angle: 15deg;
		border-width: 2px;
	}
</style>
